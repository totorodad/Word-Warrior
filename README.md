Run the gameserver.py first with pygame installed.
Then run up to four clients.

The executable is a pyinstaller version for the client only.  You need .exe, the assets directory and the _internals directory.
